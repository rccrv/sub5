meta {
  name: Login (funcionario)
  type: http
  seq: 2
}

post {
  url: http://{{keycloak_host}}/realms/sub3/protocol/openid-connect/token
  body: formUrlEncoded
  auth: basic
}

headers {
  Content-Type: application/x-www-form-urlencoded
}

auth:basic {
  username: compradores
  password: WiGPbgwfBBwoouEdqnVdtJVsaUG3deb2
}

body:form-urlencoded {
  username: funcionario
  password: funcionario
  grant_type: password
}

script:post-response {
  bru.setVar("funcionario_bearer_token", res.body.access_token)
}

tests {
  test("deve ser bem sucedido", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
