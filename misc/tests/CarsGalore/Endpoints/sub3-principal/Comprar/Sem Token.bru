meta {
  name: Sem Token
  type: http
  seq: 1
}

put {
  url: http://{{principal_service_host}}/comprar/{{veiculo1_placa}}
  body: none
  auth: inherit
}

tests {
  test("deve falhar", function () {
    expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
