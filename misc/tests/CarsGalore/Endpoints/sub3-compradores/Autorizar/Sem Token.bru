meta {
  name: Sem Token
  type: http
  seq: 1
}

put {
  url: http://{{compradores_service_host}}/autorizar{{comprador2_cpf}}
  body: none
  auth: inherit
}

tests {
  test("deve falhar", function () {
    expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
